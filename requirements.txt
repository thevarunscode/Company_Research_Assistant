# Vercel installs Python dependencies from this root file (uvicorn not needed there;
# for local development use backend/requirements.txt inside a venv).
fastapi>=0.115
httpx>=0.28
beautifulsoup4>=4.12
reportlab>=4.2
python-dotenv>=1.0
pydantic>=2.11
